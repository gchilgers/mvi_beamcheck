[project]
name = 'mvi_beamcheck'
version = '0.0.1'
requires-python = ">=3.11"

dependencies = [
    'pydicom>=3.0',
    'numpy>=2.4'
]

[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'

[tool.setuptools.packages.find]
where = ['src']